// import axios from 'axios';

// var getBaseUrl = () => {
//  let baseUrl = window.location.href.split(":",2).join(":");

//  baseUrl =  baseUrl.split('/',3).join('/');
 
//  baseUrl = baseUrl + ':'+process.env.REACT_APP_SERVER_PORT+'/rest/api/v1/';

//  return baseUrl;
 
// };

// var axiosConfig = {
//   baseURL: getBaseUrl(),
// };

// var axiosInstance = axios.create(axiosConfig);

// export default axiosInstance;